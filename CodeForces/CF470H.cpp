//This Code was made by Chinese_zjc_.
#include <iostream>
#include <fstream>
#include <iomanip>
#include <algorithm>
#include <vector>
#include <bitset>
#include <cmath>
#include <queue>
#include <stack>
#include <list>
#include <string>
#include <cstring>
#include <cstdio>
#include <cstdlib>
#include <cctype>
#include <map>
#include <set>
#include <ctime>
// #define debug
#define int long long
#define double long double
const double PI = acos(-1);
const double eps = 0.0000000001;
const int INF = 0x3fffffffffffffff;
signed main()
{
    std::ios::sync_with_stdio(false);
    std::cout << "[0[^$$'9>'0@>|~]['0-\\10*+]#%]r:  { int read() }" << std::endl;
    std::cout << "r;!n: { int n=read() }" << std::endl;
    for (int i = 0; i != 10; ++i)
        std::cout << "100" << char('a' + i) << ":" << std::endl;
    for (int i = 0; i != 10; ++i)
        std::cout << "n;" << i << ">[r;!" << char('a' + i) << ":]?" << std::endl;
    for (int j = 0; j != 10; ++j)
        for (int i = 1; i < 10; ++i)
        {
            char a = 'a' + i - 1, b = 'a' + i;
            std::cout << a << ";" << b << ";>[" << a << ";" << b << b << ";" << a << "::]?" << std::endl;
        }
    for (int i = 0; i != 10; ++i)
        std::cout << "n;" << i << ">[" << char('a' + i) << ";.32,]?" << std::endl;
    return 0;
}