//This Code was made by Chinese_zjc_.
#include <iostream>
#include <fstream>
#include <iomanip>
#include <algorithm>
#include <vector>
#include <bitset>
#include <cmath>
#include <queue>
#include <stack>
#include <list>
#include <string>
#include <cstring>
#include <cstdio>
#include <cstdlib>
#include <cctype>
#include <map>
#include <set>
#include <ctime>
// #define debug
#define int long long
#define double long double
const double PI = acos(-1);
const double eps = 0.0000000001;
const int INF = 0x3fffffffffffffff;
std::string read = "[0[^$$'9>'0@>|~]['0-\\10*+]#%]";
signed main()
{
    std::ios::sync_with_stdio(false);
    std::cout << read << "!n:" << std::endl;
    for (int i = 0; i != 50; ++i)
        std::cout << "n;" << i << ">" << read << "?" << std::endl;
    std::cout << "$n;O+." << std::endl;
    for (int i = 1; i != 50; ++i)
        std::cout << "n;" << i << ">[32,n;" << i << "-On;" << i << "-O+.]?" << std::endl;
    for (int i = 0; i != 50; ++i)
        std::cout << "n;" << i << ">[%]?" << std::endl;
    return 0;
}